# Chess Engine Dependencies

# Core dependencies
numpy>=1.21.0
torch>=1.9.0
python-chess>=1.9.0

# Optional dependencies for enhanced functionality
matplotlib>=3.3.0  # For plotting training history
scikit-learn>=1.0.0  # For additional ML utilities
pandas>=1.3.0  # For data manipulation

# Development dependencies (optional)
pytest>=6.0.0  # For unit testing
black>=21.0.0  # For code formatting
flake8>=3.9.0  # For linting

# Note: This chess engine is designed to work with minimal dependencies.
# The core functionality only requires numpy and torch.
# python-chess is used for PGN parsing and move validation.
# Other dependencies are optional and can be installed as needed.